import React from 'react';

export default class Statistic extends React.Component {
    render () {
        return (
            <div >
                Statistic
            </div>
        );
    }
}